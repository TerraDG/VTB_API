🚀 VTB API Security Scanner

**VTP API Security Scanner** — это инструмент для автоматического тестирования и анализа безопасности API по спецификации OpenAPI.  
Проект разработан для участия в хакатоне и демонстрирует принципы OWASP-проверок, тестирование авторизации и генерацию HTML-отчётов.

---

📋 Возможности

✅ Автоматическое чтение **OpenAPI (openapi.json)**  
✅ Выполнение **source-запросов** (например, `/auth/bank-token`)  
✅ Проверка цепочки согласий (`/account-consents/request`, `/account-consents/{id}` и т.д.)  
✅ Симуляция **атаки SQL Injection** и других валидационных тестов  
✅ Генерация наглядного **HTML-отчёта** со статусами и результатами  
✅ Поддержка **прогресс-бара** и простого графического интерфейса  
✅ Компиляция в **.jar-файл** для автономного запуска без IDE  

---

### 🔧 Требования
- **Java 17+**
- **Maven** (для сборки/ или же не обязателен)
- Наличие рядом файлов:
  - `openapi.json`
  - `endpoint-data.json`

---
💡 Как пользоваться приложением

После запуска откроется окно, где нужно ввести (данные заранее подставлены для удобства):

| Поле | Описание |
| ----------- | ----------- | 
| Team ID | Уникальный ID команды |
| Secret Key | Кодовое слово команды |

После нажатия на кнпоку "Run Scan" приложение выполнит автоматические следующие функции:
- Получение токена;
- Создание согласия;
- Проверка статуса согласия;
- Проведение тестов (валидные и невалидные запросы);
- Формирование HTML-отчёта (report.html) с возможностью выбора пути сохранения при необходимости.

Виды выполняемых тестов:
| Тест | Метод | Эндпоинт | Ожидаемый результат |
| ----------- | ----------- | ----------- | ----------- | 
| Happy Path | POST | /account-consents/request | ✅ 200 OK |
| Missing Authorization | POST | /account-consents/request | ❌ 403 Forbidden |
| Invalid Token | POST | /account-consents/request | ❌ 401 Unauthorized |
| SQL Injection Simulation | POST | /account-consents/request | ❌ 404 / 422 Expected |
| Missing X-Requesting-Bank | POST | /account-consents/request | ❌ 400 Bad Request |
| Fuzzed Permissions | POST | /account-consents/request | ❌ 422 Validation Error |


**HTML-отчёт: структура и содержимое**

После выполнения тестов создаётся файл report.html, который можно открыть в браузере.
Отчёт содержит несколько разделов:

1. Сводка
- Дата и время выполнения
- Количество эндпоинтов
- Количество успешных и неуспешных тестов
- Время выполнения в миллисекундах

2. Тестирование API
Список тестов, выполняемые для проверки API.
Каждая строка показывает:

| Метод | URL | Код | Статус | Время (мс) |
| ----------- | ----------- | ----------- | ----------- | ----------- |
| POST | ... | 200 | OK | 93 |
| GET | ... | 401 | FAIL | 210 |

3. Проверка безопасности
Список обнаруженых эндпоинтов ( к примеру эндпоинты без авторизации).

